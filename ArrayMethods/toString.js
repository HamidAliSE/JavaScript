const professions = ["Engineer", "Doctor", "Teacher", "Lawyer"];

console.log(`Professions: ${JSON.stringify(professions)}`);
console.log(
  `Professions in a single string: ${JSON.stringify(professions.toString())}`
);
